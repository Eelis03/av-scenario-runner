{
  "name": "av-scenario-runner-viz",
  "version": "0.1.0",
  "private": true,
  "description": "Browser playback of scenario traces exported by the scenario_runner Python package.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "serve": "python -m http.server 8000"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}
